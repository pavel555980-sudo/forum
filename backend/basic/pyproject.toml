[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"


[project]
name = "forum-application"
version = "1.0"
license = {file = "LICENCE"}
dependencies = [
    "dishka",
    "pydantic-settings",
    "psycopg[binary]",
    "sqlalchemy",
    "fastapi~=0.111.0",
    "alembic",
    "bcrypt",
    "sqladmin[full]",
    "pyjwt"
]


[project.optional-dependencies]
dev = [
    "ruff",
]


[project.scripts]
forum-basic-api = "forum_basic.main.api:main"
